# repositories package — data access layer
# Concrete implementations of abstract repository interfaces.
# Services import the interfaces, never the concrete classes directly.
# app.py picks the right concrete class at startup and injects it.
